PORT=3000
STRIPE_SECRET_KEY=

# Optional: only needed to allow a non-localhost frontend (e.g. your production
# domain once deployed). Any http://localhost:<port> origin is always allowed.
FRONTEND_URL=
